({

    loadModule: function ()
    {
        this.commands.registerCommand("rpg", this);
        this.commands.registerCommand("walk", this);
    }
    ,
    rpg:
    {
        perm: function () { return true; }
        ,

        code: function (src, cmd, chan)
        {
            if 
        }
    }
